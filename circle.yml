machine:
  node:
    version: 5.4.0

checkout:
  post:
    - cd client

dependencies:
  override:
    - npm install

test:
  override:
    - npm test && npm run coveralls

deployment:
  production:
    branch: master
    commands:
      - ./client/bin/deploy.sh ${PWD}
